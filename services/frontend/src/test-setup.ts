/** Global test setup — extends Vitest matchers with jest-dom assertions.
 * Consumed by: all .test.tsx files via vitest setupFiles
 * Depends on: @testing-library/jest-dom
 */
import '@testing-library/jest-dom/vitest'
